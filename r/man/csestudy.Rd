% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/csestudy.R, R/methods.R
\name{csestudy}
\alias{csestudy}
\alias{coef.csestudy}
\alias{print.csestudy}
\alias{summary.csestudy}
\title{Time-series inference for a cross-sectional event study}
\usage{
csestudy(
  data,
  depvar = "ret",
  indepvars = NULL,
  event_date,
  pre_start,
  pre_end,
  panel_var = "permno",
  time_var = "date",
  sample = NULL,
  method = c("ols", "gls"),
  npc = 100L,
  solver = c("cholesky", "woodbury"),
  verbose = TRUE
)

\method{coef}{csestudy}(object, ...)

\method{print}{csestudy}(x, ...)

\method{summary}{csestudy}(object, ...)
}
\arguments{
\item{data}{A long-form panel data frame with one row per (panel, time).}

\item{depvar}{Character scalar naming the dependent variable column
(default \code{"ret"}).}

\item{indepvars}{Character vector of independent-variable column names, or
\code{NULL} for an intercept-only model.}

\item{event_date, pre_start, pre_end}{Values of \code{time_var} identifying the
event date, the first (earliest) pre-event date, and the last (latest)
pre-event date. Require \verb{event_date > pre_end > pre_start}.}

\item{panel_var, time_var}{Character scalars naming the panel-identifier and
time columns (defaults \code{"permno"} and \code{"date"}). \code{time_var} should be a
sequential integer with non-trading days omitted.}

\item{sample}{Optional unquoted expression evaluated in \code{data} used to subset
rows before estimation (the analogue of Stata's \verb{[if]}), e.g.
\code{sample = abs(prc) > 5}. Note: like the Python implementation, the filter
is applied to the whole panel up front, so it also restricts the data used
to build the GLS covariance matrix.}

\item{method}{\code{"ols"} (default) or \code{"gls"}.}

\item{npc}{Number of principal components for the GLS covariance matrix
(default 100). Must not exceed the number of pre-event days.}

\item{solver}{GLS solver: \code{"cholesky"} (default, most precise) or
\code{"woodbury"} (faster, slightly less precise). Requires \code{method = "gls"}.}

\item{verbose}{If \code{TRUE}, print progress during the (slower) GLS pre-event
loop.}

\item{object}{A \code{"csestudy"} object.}

\item{...}{Ignored.}

\item{x}{A \code{"csestudy"} object.}
}
\value{
An object of class \code{"csestudy"}: a list with elements \code{method},
\code{params} (named event-date coefficients), \code{param_names}, \code{p_cdf}
(empirical CDF p-values), \code{p_parametric}, \code{pre_event_betas} (L x k),
\code{n_obs_event}, \code{n_obs_all}, \code{n_pre_event_days}, and \code{call}. See
\code{\link[=coef.csestudy]{coef.csestudy()}} and \code{\link[=summary.csestudy]{summary.csestudy()}}.
}
\description{
Estimates the event-period cross-sectional relationship between \code{depvar} and
\code{indepvars} and benchmarks it against the distribution of the same
relationship estimated on a window of pre-event ("pseudo-event") days. This
is the R implementation of Cohn, Johnson, Liu and Wardlaw (2026); it mirrors
the Stata \code{csestudy} command and the Python \code{CSEventStudy} class shipped in
the same repository.
}
\section{Methods (by generic)}{
\itemize{
\item \code{coef(csestudy)}: Extract the event-date coefficient vector.

\item \code{print(csestudy)}: Print a compact results table.

\item \code{summary(csestudy)}: Build a summary table (a data frame of coefficients and
p-values) with its own print method.

}}
\examples{
\dontrun{
df <- read.csv("examples/sample_data.csv")
tds <- sort(unique(df$date))
df$tdate <- match(df$date, tds) - match(17811L, tds)   # 0 at the event date

fit <- csestudy(df, depvar = "ret", indepvars = "lag_LNMV",
                event_date = 0, pre_start = -200, pre_end = -1,
                panel_var = "permno", time_var = "tdate",
                sample = abs(prc) > 5, method = "gls", npc = 100)
summary(fit)
coef(fit)
}
}
\references{
Cohn, J. B., Johnson, T. L., Liu, Z., and Wardlaw, M. I. (2026).
Past is Prologue: Inference from the Cross Section of Returns Around an
Event. \emph{Journal of Financial Economics} 180, 104278.
\doi{10.1016/j.jfineco.2026.104278}
}
